# Methodology

The probability distribution is computed by discretizing two
variables into bins.

P(i,j) = weighted counts in bin (i,j) / total counts

The free-energy surface is computed as

F(i,j) = -log(P(i,j))

This approach is commonly used in statistical mechanics
and molecular simulations to represent free energy landscapes.
